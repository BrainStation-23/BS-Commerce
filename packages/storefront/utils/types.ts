export interface accordionBody {
    title: string;
    body:string
  }

  export interface storiesBody {
    image: string;
    title: string;
    description:string
  }

  export interface CarouselBody {
    title: string;
    body:string;
    image: string;
  }