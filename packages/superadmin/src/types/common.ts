// ----------------------------------------------------------------------

export enum ActionType {
  ENABLE = 'enable',
  DISABLE = 'disable',
}

export enum SnackbarVariant {
  WARNING = 'warning',
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum DialogType {
  ENABLE = 'enable',
  DISABLE = 'disable',
}
