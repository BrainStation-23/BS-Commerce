import { Meta } from "@storybook/addon-docs";

<Meta title="Introduction" />

# Atomic Components

### How to use

- Create your atomic component in `Components` directory
- Export component from `/Components/index.tsx`
- Import in your app
