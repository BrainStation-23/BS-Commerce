'use strict';

angular.module('mean.shopAdmin').controller('categoryCreateController', ['$scope', 'Global',
    function($scope, Global) {
        //$scope.catId = $stateParams.catId;
        //console.log($scope.catId);
    }
]);