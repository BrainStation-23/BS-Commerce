'use strict';

angular.module('mean.shopAdmin').controller('categoryAddController', ['$scope', 'Global', '$stateParams',
    function($scope, Global, $stateParams) {
        //$scope.catId = $stateParams.catId;
        //console.log($scope.catId);
    }
]);